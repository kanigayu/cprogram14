#include<stdio.h>
int main()
{
int base,power;
int answer=1,i=0;
printf("Enter the base")
scanf("%d",&base);
printf("Enter the power");
scanf("%d",&power);
{
for(i = 0;i<power;i++)
{
        answer= answer * base;
    }
    printf("power is%d",answer);
    }
    retuen 0;
    }
    
